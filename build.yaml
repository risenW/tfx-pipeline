apiVersion: skaffold/v1beta13
build:
  artifacts:
  - context: .
    docker:
      dockerfile: Dockerfile
    image: gcr.io/tfx-project-288611/house_price_pipeline
  tagPolicy:
    envTemplate:
      template: latest
kind: Config
